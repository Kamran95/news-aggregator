export interface DateRange {
    startDate: string | null;
    endDate: string | null;
}
export interface DropdownOptionsTypes {
    label: string;
    onClick: () => void;
}
export interface DropdownOptions {
    id: string;
    label: string;
}
